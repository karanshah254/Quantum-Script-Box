let name = 'Karan';

// this above line checks if "name" is "falsy or NULL". 
//If it is falsy or NULL it will prints value next to "OR" operator else it will print "name" 
let userName = name || 'New User';
console.log(userName); 